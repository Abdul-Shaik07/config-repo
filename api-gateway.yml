server:
  port: 8080

jwt:
  secret: 6d4217192951e97f82b3db4e527346806bc7b9a5b5cb455728219cc618d2cc89


spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webflux:
          routes:
            # ---------------- AUTH SERVICE ----------------
            - id: auth-service
              uri: lb://AUTH-SERVICE
              predicates:
                - Path=/auth/api/**
            # ---------------- PRODUCT SERVICE ----------------
            - id: product-service
              uri: lb://PRODUCT-SERVICE
              predicates:
                - Path=/products/api/**
            # ---------------- CART SERVICE ----------------
            - id: cart-service
              uri: lb://CART-SERVICE
              predicates:
                - Path=/cart/api/**
            # ---------------- ORDER SERVICE ----------------
            - id: order-service
              uri: lb://ORDER-SERVICE
              predicates:
                - Path=/order/api/**
