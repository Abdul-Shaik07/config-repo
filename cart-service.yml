server:
  port: 8083
spring:
  application:
    name: cart-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cart_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
